{
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:frontend": "cd front-end && npm run serve",
    "dev:backend": "cd back-end && npm run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
